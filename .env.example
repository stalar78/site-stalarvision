# Enable network access to the Vite dev server only when you explicitly need it.
VITE_DEV_ALLOW_NETWORK=false

# Optional comma-separated hosts/IPs allowed for external access in dev mode.
# Example: VITE_DEV_ALLOWED_HOSTS=192.168.1.15,stalarvision.local
VITE_DEV_ALLOWED_HOSTS=
